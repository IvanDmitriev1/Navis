<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Navis.WinUI.Sample.Views.MainShell.ExamplesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Navis.WinUI.Sample.Models"
    xmlns:nav="using:Navis.WinUI">

    <nav:TitleBarNavigation.Content>
        <TextBlock Text="Examples" />
    </nav:TitleBarNavigation.Content>

    <ScrollViewer
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto">
        <StackPanel
            MaxWidth="760"
            Padding="32,24"
            HorizontalAlignment="Center"
            Spacing="16">
            <StackPanel Spacing="8">
                <TextBlock
                    Style="{StaticResource TitleTextBlockStyle}"
                    Text="Navigation examples" />
                <TextBlock
                    Text="Explore the navigation capabilities demonstrated by this sample."
                    TextWrapping="Wrap" />
            </StackPanel>

            <ListView
                x:Name="ExamplesList"
                AutomationProperties.AutomationId="ExamplesList"
                AutomationProperties.Name="Navigation examples"
                ItemsSource="{x:Bind ViewModel.Examples}"
                SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:NavigationExample">
                        <Border
                            Margin="0,0,0,12"
                            Padding="20"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                            <Grid ColumnSpacing="20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="8">
                                    <TextBlock
                                        Style="{StaticResource SubtitleTextBlockStyle}"
                                        Text="{x:Bind Title}" />
                                    <TextBlock
                                        Text="{x:Bind Description}"
                                        TextWrapping="Wrap" />
                                    <TextBlock
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="{x:Bind ApiSurface}"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                                <Button
                                    Grid.Column="1"
                                    AutomationProperties.AutomationId="{x:Bind Title}"
                                    AutomationProperties.Name="{x:Bind Title}"
                                    Click="OpenExampleButton_Click"
                                    Content="Open"
                                    VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </ScrollViewer>
</Page>
